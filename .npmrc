# Don't re-run `pnpm install` before every `pnpm run <script>`.
# Our scripts (db:reset, db:test) shell out to the local supabase binary; the
# pre-run install check otherwise fails on un-built optional deps (sharp, etc.).
verify-deps-before-run=false
